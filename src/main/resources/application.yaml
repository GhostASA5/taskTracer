spring:
  application:
    name: Task-tracer

  jpa:
    generate-ddl: true
    open-in-view: true
  datasource:
    url: jdbc:postgresql://localhost:5437/task_db
    username: postgres
    password: postgres

server:
  port: 8088

springdoc:
  api-docs:
    path: /api-docs